header:
  license:
    spdx-id: Apache-2.0
    copyright-owner: foyer Project Authors

  paths:
    - "**/*.rs"

  # ScopeDB-owned sources vendored by the leiysky fork retain their original license.
  paths-ignore:
    - "foyer-extent/**"
    - "foyer-fixed-lsm/**"

  comment: on-failure
